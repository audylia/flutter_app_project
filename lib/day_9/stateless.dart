import 'package:flutter/material.dart';

class StatelessDay9 extends StatelessWidget {
  const StatelessDay9({super.key});

  @override
  Widget build(BuildContext context) {
    return Text(
      'Contoh Stateless',
      style: Theme.of(context).textTheme.headlineMedium,
    );
  }
}
